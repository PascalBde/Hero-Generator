{
  "name": "character-generator",
  "version": "0.1.0",
  "private": true,
  "main": "main.js",
  "dependencies": {
    "file-dialog": "0.0.7",
    "react": "^16.8.6",
    "react-accessible-accordion": "^3.0.0",
    "react-dom": "^16.8.6",
    "react-feather": "^2.0.3",
    "react-scripts": "3.0.1",
    "react-tabs": "^3.0.0",
    "save-file": "^2.3.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "electron": "electron . --disable-gpu",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "build:win": "electron-packager ./ --app-version=1.0 --app-copyright=PascalBurg  --arch=ia32 --platform=win32 --asar --ignore=.idea --out=electronBuild/ --overwrite=true  --build-version=1.0 --app-bundle-id=Marvelator",
    "build:linux": "electron-packager ./ --app-version=1.0 --app-copyright=PascalBurg  --arch=x64 --platform=linux --ignore=.idea --out=electronBuild/ --overwrite=true --build-version=1.0 --app-bundle-id=Marvelator"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "electron-packager": "^14.0.4",
    "electron": "^15.5.5"
  }
}
